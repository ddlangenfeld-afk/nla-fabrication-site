# Generated by knob_model.py — rebuild with:
#   python3 design/knob_model.py
#   python3 design/knob_model.py --no-line --out design/hvac-slider-knob-blank
# The .obj is committed because it is text and imports straight into Blender.
# The .stl is 1.2 MB of binary that regenerates in under a second, so it isn't.
*.stl
hvac-slider-knob-blank.obj
__pycache__/
